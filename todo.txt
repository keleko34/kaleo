- look into using the width/height of the canvas element as the render chain
- add the ability to remove debug code on build
- add a settings menu for binding keys, allow saving of the binds
- change inputs to allow registering of an action and then later bind to a key
- add seperate gui context in input
- move engine plugin entirely to the engine

